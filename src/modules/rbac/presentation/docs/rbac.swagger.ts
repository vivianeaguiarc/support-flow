/**
 * @swagger
 * tags:
 *   - name: RBAC Admin
 *     description: |
 *       Gestão enterprise de papéis e permissões por tenant.
 *
 *       Requer permissão `roles.manage` (ou `SUPER_ADMIN`).
 *
 *       Endpoints:
 *       - `GET/POST /admin/roles`
 *       - `PATCH/DELETE /admin/roles/{id}`
 *       - `POST /admin/roles/{id}/permissions`
 *       - `GET/POST /admin/permissions`
 */
